function solve(firstInput, secondInput, thirdInput) {
    let sum = firstInput.length + secondInput.length + thirdInput.length;
    let averageLength = Math.floor(sum / 3);

    console.log(sum);
    console.log(averageLength);
}