function sumFirstLast(elements) {
    let firstElement = Number(elements[0]);
    let lastElement = Number(elements[elements.length - 1]);
    let sum = firstElement + lastElement;

    console.log(sum);
}